

# Modes
- `nmap`, `nunmap` normal mode
- `vmap`, visual mode
- `imap`, insert mode
- `nnoremap`, nomral mode, no recursive


## special key
- `<C-D>` means `ctrl` + `d`
- `<SPACE>` means the space key
- `<ESC>`
